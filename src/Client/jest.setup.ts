/**
 * Глобальная настройка после инициализации тестового окружения.
 *
 * Здесь подключаются расширения для функции `expect`.
 * Например: expect(element).toBeInTheDocument()
 *
 * Библиотека @testing-library/jest-dom предоставляет такие методы.
 */

import '@testing-library/jest-dom';