import { MultimodeControl, MultimodeControlProps } from '../multi-mode-control/multi-mode-control';

export const StringControl: React.FC<MultimodeControlProps> = (props) => <MultimodeControl {...props} />;
