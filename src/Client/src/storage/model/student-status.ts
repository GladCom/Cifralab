import { StringControl } from '../../components/shared/control/string-control';

const model = {
  name: { name: 'Статус студента', type: StringControl, show: true, required: true },
};

export default model;
