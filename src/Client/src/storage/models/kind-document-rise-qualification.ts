import StringControl from '../../components/shared/business/common/string-control';

const model = {
  name: { name: 'Вид документа повышения квалификации', type: StringControl, show: true, required: true },
};

export default model;
