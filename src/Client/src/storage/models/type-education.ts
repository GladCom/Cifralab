import StringControl from '../../components/shared/business/common/string-control';

const model = {
  name: { name: 'Тип образования', type: StringControl, show: true, required: true },
};

export default model;
